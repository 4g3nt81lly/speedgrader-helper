import SharedConstants from '#shared/constants';

const Constants = {
	...SharedConstants,
	APP_SETTINGS_ACTION_QUEUE_NAME: 'appSettings.queue',
	QUIZZES_ACTION_QUEUE_NAME: 'quizzes.queue',
};

export default Constants;
